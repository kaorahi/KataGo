Get the latest Eigen from https://gitlab.com/libeigen/eigen (not 1.3.7 but 20-08-05 01:46 46f8a18).

=================

In this directory (src/cpp/neuralnet/from_tensorflow)...

curl -L https://github.com/tensorflow/tensorflow/archive/v2.3.0.tar.gz | tar xvz tensorflow-2.3.0/{LICENSE,tensorflow/core/kernels/eigen_{spatial_convolutions-inl.h,convolution_helpers.h}}

patch -p0 < modify_path.patch

=================

Example of compiler options for katago:

cmake . -DUSE_BACKEND=EIGEN -DCMAKE_CXX_FLAGS='-march=native -mfpmath=both'

If you did not install Eigen globally:

cmake . -DUSE_BACKEND=EIGEN -DCMAKE_CXX_FLAGS='-I/FOO/BAR/eigen -march=native -mfpmath=both'

You will need to modify "-march=native" if you are planning to distribute the binary for others.
